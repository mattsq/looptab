# M31 — shared-readout confound control: ✅ COMPLETE (2026-07-14 10:40)

**Goal.** Run M30's named-but-unbuilt control: separate the mixer's two advantages over trm_flat/ff —
(i) cross-cell token-MIXING vs (ii) the per-cell SHARED readout — which M30 could not disentangle.

**What was built.**
- `disable_token_mix` flag on `TRMMixer` (off by default ⇒ committed mixer byte-identical; unit-tested)
  + `TRMMixerNoMix` subclass, registered `trm_mixer_nomix`; wired into `run.py` `_build_model`.
- 4 new unit tests (bit-identical guard, drops-only-token-mix, cells-don't-communicate, registry+determinism).
- Budget probe (`probe_budget.py`): nomix hidden re-widened to match trm_flat (ratios 0.998–1.003);
  cross-checks that trm_mixer widths reproduce M30 exactly.
- 6 lean configs (`m31_{etth1,weather}_h{192,336,720}.yaml`), 4 arms {trm_mixer, trm_mixer_nomix,
  trm_flat, ff_matched}, generated by `gen_configs.py`. Dropped M30's 2 untied arms (nomix resolves
  the confound alone). Runner `run_m31_sweep.sh` (resumable, caffeinated).

**Result (all 6 cells, 10 blocks each, budget-clean; MSE Δ, negative favours first arm):**
The confound resolves against mixing — the FIRM claim is the negative one (it is NOT mixing).
- Δ(mixer − nomix) [MIXING] mean-POSITIVE everywhere (+0.03→+0.16 = mixing adds nothing; weakly harmful),
  sign-sig against mixing on all 3 weather cells (10/0, 9/1, 10/0) but NULL on etth1 (8/2, 5/5, 8/2).
  "Worsens with horizon" is a MEAN artifact — non-monotone by median (weather 0.06→0.14→0.12).
- Δ(nomix − ff) [SHARED-READOUT/CI] the whole win — all 0/10–1/9, p≤.021; weather MEDIAN amplification
  −0.24→−0.43→−0.65 (the −1.13 h720 MEAN is ~1.7× inflated by ONE ff outlier block at MSE 6.60).
- nomix (channel-INDEPENDENT) is the BEST arm in every cell by mean (beats mixer/flat/ff/persistence).
- M30 reproduced to ROUNDING (trm_flat/ff bit-identical per-seed; trm_mixer weather-h720 drifts 2.6e-3 =
  3-D-matmul non-determinism at max width — NOT strictly bit-identical).

**Adversarial review (subagent, 2026-07-14):** verdict HOLDS WITH CAVEATS. Confirmed the core
(mixing not the mechanism) is robust; flagged 3 overclaims now fixed in all docs — (1) weather-h720
amplification is single-block-inflated (report median), (2) mixing leg not byte-clean (matched to
trm_flat, ≤1.6% width gap), (3) "M30 bit-reproduced" false for trm_mixer weather-h720. Non-issues:
code correctness, channel-independence of nomix (verified by perturbation probe), stats plumbing.

**Write-up landed:** `results/log/m31.md`, `results/LOG.md` index row, CLAUDE.md §11.1 (arm), §11.2
new finding #13 + corrections to #8/#10 + the §4 ⭐ block, §11.3 (control DONE; more-datasets +
benchmark-scale + optional ingredient-split remain open), §11.4 (closed lever).

**Canonical results:** `results/m31_{etth1,weather}_h{192,336,720}_*` (curve/deltas/params/json).
Full run log: `scratchpad/m31/m31_sweep.log`. Nothing committed (results raw/gitignored per repo convention).

**Scope of the correction: FORECASTING ONLY.** Synthetic constraint-coupled mixing wins (Sudoku/
converge/hopfield/disruption) untouched. Caveat: Δ(mixer−nomix) cleanly isolates mixing; Δ(nomix−ff)
bundles shared-readout + channel-independence + per-variable weight-sharing (optional future control
to split them, §11.3).
