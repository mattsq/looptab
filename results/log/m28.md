# M28 — the cross-cell MIXER re-test carried to the last two untested tasks (`mixed_converge`, `nested_converge`)

**Why.** A repo audit against the M23→M26 mixer arc found exactly two synthetic tasks that fit the mixer's
requirements (multi-output + cross-cell coupled + `in_features % out_features == 0`) but had NEVER been run
against `trm_mixer`: `mixed_converge` (M15, the last MED-structure task, flagged "STILL untested" in §11) and
`nested_converge` (M17, the Task C substrate — whose M18 "plateau" verdict was measured entirely on the flat
loop). Everything else is covered (converge/iterated M24, hopfield M24c, multi_parity M24d, disruption M24f,
sudoku M23, multilabel M25, forecasting M26); `linear`/`parity` are single-output (no cell structure ⇒ no
mixer). Recipe = the M24 lean isolation + the M24b mechanism ablation + the M24e tying attribution: same
training (standard train + deep supervision + RMSNorm + full BPTT), the mixing operator the only changed
variable vs `trm_flat`, `distractors: 0` on every arm (mixer divisibility), 8 seeds, `num_threads=1` (3-D
matmul). **Purely additive — no source/test changes; all M0–M27 results bit-identical.** Budget parity ✓
within ±5% on every matched arm, both tasks (verified in-run). **Caveat that bounds both tasks:** setting
`distractors: 0` (mixer-required) differs from the M15/M18 configs (`distractors: 8`), so the per-arm numbers
here are NOT directly comparable to those historical flat runs — the within-config Δ is the result (exactly
as M24 handled the earlier converge configs). One consequence surfaces below (the flat loop beats ff on
`mixed_converge` at distractors=0, unlike M15's distractors=8 tie).

### M28a — `mixed_converge` (rule_set {78,92,141,197}, T=6, w∈{24,32}): the mixer (near-)SOLVES the non-uniform task; M15's leg-2 "needs a UNIFORM rule" does NOT survive removing distractors — but that is CONFOUNDED with the distractors change, NOT cleanly a flat-architecture artifact.

Per-arm EM (w24 / w32): ff **0.510 / 0.105**, trm_flat 0.671 / 0.402, **trm_mixer 0.949 / 0.880**,
trm_decoupled 0.056 / 0.007, untied_matched 0.081 / 0.008; (tying config) untied_mixer_matched 0.901 / 0.791,
untied_mixer (7× ceiling) 0.948 / 0.945.

- **HEADLINE — the mixer (near-)solves the per-position MIXED (non-uniform) CA:** Δ(trm_mixer − ff) acc
  **+0.028 / +0.069**, EM **+0.440 / +0.774**, both 8/0, p=.008, **GROWING with w** (the M24 signature; mixer EM
  0.949 solves at w24, 0.880 ≈12% rows wrong at w32 so "near-solves" there). *EM-floor caveat (M24f consistency):*
  the huge EM Δ rests on a small ACCURACY gain (+0.028 / +0.069) and Δ(mixer−ff) coherence_excess is **≤ 0**
  (−0.024 / −0.003) — a raw per-cell-accuracy win amplified by whole-row EM, NOT a coherence win.
  **What this does and does NOT show about M15's leg-2:** M15 established (on the flat loop, at `distractors: 8`)
  that leg-2 "loop-beats-the-MLP" does NOT reproduce on the non-uniform mixed task without a uniform rule. Here
  the mixer beats ff by a wide margin — BUT so does the flat loop (Δ(trm_flat − ff) EM +0.162 / +0.296, 8/0; see
  the distractors caveat). So the uniform-rule requirement **dissolves for BOTH architectures once distractors are
  removed** ⇒ the dissolution is CONFOUNDED with the `distractors: 8→0` change (uncontrolled vs M15), and is **NOT
  cleanly a flat-architecture artifact** — the flat architecture is not the limitation (it wins at distractors=0),
  and M28a ran no uniform arm at distractors=0 to re-establish the mixed-vs-uniform contrast. A clean test would
  re-run BOTH mixed and a matched-depth uniform CA at distractors=0. What IS clean here is that the mixer handles
  NON-uniform local coupling (the mixing marginal below), consistent with M24's "cross-cell DEPENDENCY, not
  rule-uniformity, is the axis."
- **Mixing marginal (architecture-clean, pivot on the flat loop):** Δ(trm_mixer − trm_flat) acc +0.017 / +0.027,
  EM **+0.278 / +0.478**, 8/0, growing.
- **PRIMARILY the mixing ARCHITECTURE, not recurrence (M24e/M24f pattern):** the non-recurrent budget-matched
  Δ(untied_mixer_matched − ff) already gives acc +0.026 / +0.065, EM +0.391 / +0.686 (8/0); tying is only a
  small efficiency edge Δ(trm_mixer − untied_mixer_matched) EM +0.049 / +0.089 (EM 8/0 both widths; acc 7/0 @w24,
  8/0 @w32); and the 7×-param ceiling MATCHES/BEATS the tied mixer — Δ(trm_mixer − untied_mixer) EM +0.002 ns
  (w24) / **−0.065, 0/8** (w32).
- **§9.2 legs reproduce:** leg-1 Δ(trm_flat − trm_decoupled) acc +0.119 / +0.131 (8/0), P1 Δ(trm_flat −
  untied_matched) acc +0.088 / +0.134 (8/0).
- **Distractors caveat (flagged, not a mixer effect):** at `distractors: 0` the FLAT loop also beats ff here —
  Δ(trm_flat − ff) EM +0.162 / +0.296 (8/0) — unlike M15's distractors=8 *tie*. So the clean architecture
  statement is the **mixing marginal** Δ(mixer − flat), not "mixer beats ff where flat ties" (which does not
  hold at distractors=0). The mixer-solves-it verdict is robust either way (EM 0.88–0.95 vs ff 0.10–0.51).

**M28a verdict:** the mixer (near-)solves the non-uniform mixed CA (the CA-`converge` M24 result extends to the
spatially non-uniform member); it is primarily the mixing architecture (tying = efficiency, ceiling ties the
tied loop) — closing the last MED-structure task on the CROSS-CELL-COUPLED side. On M15's leg-2 "uniform rule"
requirement the honest reading is WEAKER than a flat-architecture claim: the requirement dissolves at
distractors=0 for the flat loop too, so its disappearance is confounded with the distractors change (not
isolated to the architecture, and no uniform arm was run to re-test the contrast).
Canonical: `m28a_mixed_converge_mixer_20260706T212812_*`, `m28a_mixed_converge_untied_20260707T001032_*`.

### M28b — `nested_converge` (inner 13 / outer 79 / block_w 8, T=6, n_blocks∈{3,4}⇒w 24/32) @ 4k: the mixer IMPROVES but does NOT solve, and the ceiling is SHARED by the non-recurrent mixer ⇒ §9.3 re-gate NOT triggered, Task C stays CLOSED.

Per-arm EM (w24 / w32): ff 0.715 / 0.567, trm_flat 0.728 / 0.536, **trm_mixer 0.791 / 0.697**, trm_decoupled
0.619 / 0.437, untied_matched 0.641 / 0.480; (tying) untied_mixer_matched 0.813 / 0.662, untied_mixer 0.825 /
0.722.

- **The mixer beats ff but does NOT crack the target:** Δ(trm_mixer − ff) acc +0.040 / +0.052, EM
  **+0.076 / +0.130**, 8/0, growing — yet trm_mixer EM 0.70–0.79 stays **far below 1.0** (unlike `converge`
  /`mixed_converge`, where the mixer SOLVED). nested is a genuinely harder two-timescale target and at 4k it
  is sample-limited (M18j: data is the lever).
- **The DECISIVE re-gate read — the ceiling is architecture-shared, NOT recurrence-specific:** the
  non-recurrent budget-matched Δ(untied_mixer_matched − ff) gives the WHOLE edge (acc +0.039 / +0.034, EM
  +0.098 / +0.095, 8/0); tying is NS on nested — Δ(trm_mixer − untied_mixer_matched) EM −0.022 (1/7, w24) /
  +0.035 (7/1, p=.07, w32); and the 7× ceiling matches/beats the tied mixer — Δ(trm_mixer − untied_mixer) EM
  −0.035 (0/8, w24) / −0.025 (ns, w32). So a NON-recurrent mixer of equal/greater capacity sits at the same
  ~0.7–0.8 plateau. **The §9.3 re-gate condition (loop plateaus below target AND the non-recurrent control
  does NOT share the ceiling) is NOT met** — the mixer's nested ceiling is exactly the kind of shared
  capacity/data wall M18g found for the flat loop, now shown to hold for the mixing architecture too. **Task C
  stays firmly closed; do NOT build M19.**
- **Flat loop ≈ ff (M18g replication holds at this recipe):** Δ(trm_flat − ff) NS — acc +0.002 (4/4, p=1.0,
  w24) / −0.018 (1/7, p=.07, w32).
- **§9.2 legs reproduce on the two-timescale family:** leg-1 Δ(trm_flat − trm_decoupled) EM +0.109 / +0.098
  (8/0 EM), P1 Δ(trm_flat − untied_matched) acc +0.036 / +0.022 (8/0).

**Data sweep (n_train 4k→16k→64k, lean triple) — DONE; traces the mixer's nested ceiling against M18j's
flat-loop curve (EM 0.75→0.93→0.99).** trm_mixer EM by n_train (w24 / w32): **4k 0.791 / 0.697 → 16k 0.930 /
0.898 → 64k 0.990 / 0.985**; ff over the same: 0.715 / 0.567 → 0.862 / 0.742 → 0.964 / 0.909; flat: 0.728 /
0.536 → 0.891 / 0.789 → 0.959 / 0.905. **The mixer's ceiling LIFTS with data toward ~1.0 (EM 0.99/0.985 @64k)**
— the same data-lifts-the-ceiling pattern M18j found for the flat single loop (which reached 0.99 @64k there;
M28b's OWN flat loop reaches 0.959 / 0.905 @64k, i.e. the arms have NOT fully co-saturated here). So the nested
plateau is a SAMPLE wall for the MIXING architecture too, NOT a timescale deficit ⇒ **data (not a second
timescale) is the lever; Task C stays closed** (§9.3's own null clause: the single-timescale model reaches the
nested target with enough data). The mixer keeps a modest edge over ff across the whole sweep that SHRINKS as
the arms co-saturate but is NOT yet gone at 64k — Δ(mixer−ff) EM +0.076/+0.130 @4k (8/0) → +0.067/+0.157 @16k
(8/0) → +0.025/+0.076 @64k (6/0, p=.03, still significant, esp. w32) — the mixing operator helps but does not
change the sample-bound nature; flat ≈ ff throughout (Δ(flat−ff) ns at 64k). NB the decisive evidence for
"sample-bound, not timescale" is the 4k untied-mixer control (shared ceiling), NOT the 64k point. (64k at 6
seeds ⇒ sign test clears §5's exact-binomial floor, p=0.03125.) Canonical
(sweep): `m28b_nested_data16k_20260707T093903_*`, `m28b_nested_data64k_20260708T025620_*`.
Canonical (4k): `m28b_nested_converge_mixer_20260707T020203_*`, `m28b_nested_converge_untied_20260707T044402_*`.

**M28 net:** the mixer re-test now covers every cross-cell-coupled synthetic task. `mixed_converge` joins the
mixer-WIN side (mixer near-solves it; and leg-2's uniform-rule requirement does not survive at distractors=0 —
though that dissolution is confounded with the distractors change, not cleanly a flat-architecture result).
`nested_converge` is the informative exception — the mixer improves the operator but the two-timescale target's
4k ceiling is a shared capacity/data wall (non-recurrent mixer sits at it too; data lifts it to ~solve at 64k),
so the mixer does NOT re-open Task C. Both confirm the M24e/M24f attribution: it is the mixing ARCHITECTURE,
with tying = efficiency (present on `mixed_converge`, absent on `nested_converge`).

**M28 adversarial review (this branch):** confirmed additivity (only CLAUDE.md/LOG.md + new configs/CSVs changed;
all six JSON records carry the pre-M28 git_sha 77de167), determinism (num_threads=1 recorded in every run),
budget parity (every matched arm within_tol; ceiling correctly role-flagged at ~7.2–7.4×), all spot-checked
numbers, and the 6-seed 64k sign test (p=0.03125). It caught one MAJOR overstatement — the M28a "leg-2 was a
FLAT-architecture artifact" claim is confounded by the uncontrolled distractors=8→0 change (the flat loop also
beats ff at distractors=0), now reworded above to the honest "confounded, not isolated" reading — plus three
MINORs (EM-floor/coh_excess≤0 caveat added; a (7/0,8/0) sign-count mislabel corrected to EM 8/0 both widths;
"exact M18j signature" softened, noting M28b's own flat loop reaches 0.959/0.905 not 0.99 at 64k). The nested /
Task-C-stays-closed conclusion was judged SOUND; the M28a mixer-(near-)solves result is real, reproducible, and
budget-clean, with only the leg-2 attribution walked back.
