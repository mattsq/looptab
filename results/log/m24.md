# M24 — DONE. The M23-MIXER re-test carried to the ORIGINAL ring tasks (`converge` + `iterated`). The verdict SPLITS along the convergent/non-convergent axis: on the CONVERGENT fixed-point ring (`converge`) the mixer is a decisive, SIGNIFICANT, GROWING-with-difficulty win that SOLVES the task where the flat loop and ff only degrade — so the CA `converge` negatives WERE a flat-architecture artifact; on the NON-CONVERGENT moving CA (`iterated`, rule 110) the mixer delivers a large per-step operator win at the learnable depth (T=4) but does NOT crack the M3a depth wall at T≥8 — the mixer improves the operator, it does not confer depth-extrapolation.

M23's MIXER re-test overturned the Sudoku negative and flagged the important open question: **every M0–M22 negative used the FLAT `trm`, so "loop ≈ feedforward on structured synthetic tasks" may be a flat-architecture artifact** on any task with structure a mixer can exploit (CLAUDE.md §11c named the CA/`converge`/`hopfield` family the highest-value re-test). This milestone runs the LEAN M23 isolation — 3 arms (`trm_mixer` vs `trm_flat` vs `ff_matched`), same training for all, the cell-mixing operator the ONLY changed variable — on the two HIGHEST-value 1-D ring tasks. Recipe matched to the M23 winner (standard train + deep supervision + RMSNorm + full BPTT); `distractors: 0` for all arms (the mixer requires `in_features % out_features == 0`; kept identical so the comparison stays fair); 8 seeds; `num_threads=1` (mixer 3-D matmul). Configs `configs/experiments/m24_mixer_{converge,iterated}.yaml`.

**`converge` (rule 78, fixed-point ring; sweep w∈{24,32,48}; 8 seeds, 100 epochs, EM headline):**
| w | trm_mixer acc / EM | trm_flat acc / EM | ff acc / EM | Δ(mixer−ff) EM | Δ(mixer−flat) EM | Δ(flat−ff) EM |
|---|---|---|---|---|---|---|
| 24 | 0.999 / ~1.00 | 0.982 / — | 0.976 / — | **+0.345 (8/0, p=.008)** | **+0.204 (8/0, p=.008)** | +0.142 (8/0) |
| 32 | 0.998 / ~1.00 | 0.962 / — | 0.950 / — | **+0.676 (8/0, p=.008)** | **+0.538 (8/0, p=.008)** | +0.138 (8/0) |
| 48 | 0.998 / ~1.00 | 0.934 / — | 0.889 / — | **+0.942 (8/0, p=.008)** | **+0.859 (8/0, p=.008)** | +0.083 (8/0) |

**Findings (converge).**
1. **The mixer SOLVES `converge` at every width** (token-acc ~0.998 ⇒ EM ≈ 1.0), while the flat loop and ff degrade with w (acc 0.982→0.934 flat, 0.976→0.889 ff ⇒ their EM collapses because 0.93^48 ≈ 0.03). Δ(mixer−ff) and Δ(mixer−flat) on EM are significant (8/0, p=.008) at every width **and GROW monotonically with w** — the M23 "advantage widens with difficulty" signature, on a ring CA. Token-acc Δ is likewise 8/0 everywhere (+0.022→+0.049→+0.109 vs ff).
2. **It is the OPERATOR, not loop-vs-ff and not scale.** The flat loop's leg-2 coherence edge over ff (M9) reproduces (Δ(flat−ff) EM +0.14/+0.14/+0.08, 8/0) but is DWARFED by the mixer — the mixer turns a marginal ~0.08–0.14 EM edge into a task-SOLVING +0.35–0.94 EM win. So the small/marginal CA `converge` loop-edge the whole M8–M15 program characterized was **the flat architecture leaving the win on the table**: given a cell-mixing update, the loop essentially solves the family.
3. **Conservative at w=48** (budget audit flags mixer ratio 0.866 — the same documented conservative under-budget pattern as untied in M3a/M13/M22): the mixer WINS at w=48 with ~13% FEWER params, so the largest edge is if anything understated.

**`iterated` (rule 110, non-convergent moving CA; sweep T∈{4,8,12} with `couple_n_steps_to_param: T`; 8 seeds, 100 epochs, token-acc headline — EM≈0 for all arms except the mixer at T=4):**
| T | trm_mixer acc / EM | trm_flat acc | ff acc | Δ(mixer−ff) acc | Δ(mixer−ff) EM |
|---|---|---|---|---|---|
| 4 | **0.903 / 0.11** | 0.661 | 0.665 | **+0.238 (8/0, p=.008)** | **+0.109 (8/0, p=.008)** |
| 8 | 0.566 / 0.00 | 0.533 | 0.550 | +0.016 (7/1, p=.07, ns) | 0.000 |
| 12 | 0.533 / 0.00 | 0.528 | 0.533 | −0.000 (4/4, ns) | 0.000 |

**Findings (iterated).**
1. **A LARGE, significant per-step operator win at T=4** where the flat arms tie each other: mixer acc 0.903 vs flat 0.661 ≈ ff 0.665 (Δ(mixer−ff) +0.238, Δ(mixer−flat) +0.242, both 8/0, p=.008; EM +0.109, 8/0 — the mixer gets ~11% of whole 24-cell rows exactly right where flat/ff get ~0). `trm_flat ≈ ff` (Δ −0.004, 4/4, p=1.0) — the M3a "flat loop = feedforward" replicated, and again it is the mixing operator that breaks the tie.
2. **The mixer does NOT crack the depth wall.** At T≥8 the M3a wall (the s0→s_T moving target is near-unlearnable one-shot for every arm) reasserts: all three arms collapse to ~baseline (0.53–0.57), the mixer's edge shrinks to +0.016 (ns) at T=8 and vanishes at T=12. So the mixer improves the *operator* (a much better single-/few-step CA predictor) but confers **no depth-extrapolation / long-horizon algorithm-tracking** on a non-convergent target — the advantage is LARGEST at T=4 and vanishes, the OPPOSITE of "grows with difficulty."

**Net verdict (M24).** The mixer generalizes off Sudoku to the ring CA family, but ALONG THE CONVERGENT AXIS the program already identified as the loop's home (§9.2): on the **convergent fixed-point** ring (`converge`) it is a decisive, growing, task-solving win — the CA `converge` "small/marginal loop edge" was a flat-architecture artifact; on the **non-convergent moving** CA (`iterated`) it improves the per-step operator but does NOT overturn the depth-extrapolation negative (M1/M3a/M7). This is fully consistent with the whole-project finding that the loop's value is on fixed-point/convergent targets, not depth (M8/M21) — the mixer amplifies the former without unlocking the latter. **So of the M0–M22 negatives, the ones now shown to be flat-architecture artifacts are the *coherence-on-convergent-ring* ones (the mixer solves them); the *depth-extrapolation* negatives survive the mixer.** Rigor: budget-matched (converge within ±5% at w≤32, conservative under-budget at w48; iterated within ±0.3%); one knob (operator) vs the flat trm; 8 seeds, exact sign test; `num_threads=1`. Canonical: `m24_mixer_converge_20260703T012300_*`, `m24_mixer_iterated_20260703T004448_*`.

**Open follow-ups (NOT run — the honest next probes).** (a) The §9.2 mechanism ablations (`trm_decoupled` joint-state, `untied_matched` tying) were deliberately deferred (lean 3-arm first) — worth adding on `converge` to ask whether the mixer's win is still a *joint-state* effect or a new capability [**DONE — M24b below**]; (b) `hopfield` (dense, no local structure — the expected NULL control that mixing needs *local* structure) and `mixed_converge`/`disruption` (MED structure) remain untested; (c) `parity`/`multi_parity` (exchangeable features — the negative control where a mixer should NOT help) would confirm the win is structure-specific, not generic capacity. Per §11c these are gated on this positive headline, now met for the convergent-ring case.

### M24b — MECHANISM ablation of the M24 converge win: the mixer's advantage is a DISTINCT capability, not leg-1 done better — joint-state (leg-1) + tying (P1) dominate at small w and FADE, the MIXING operator's marginal GROWS and carries the win exactly where joint-state coherence runs out (w≥32)

Added the two §9.2 mechanism controls (`trm_decoupled` = per-cell independent latents, the leg-1 ablation; `untied_matched` = untied stack at budget, the P1/tying control) to the exact M24 `converge` setup (rule 78, distractors 0, sweep w∈{24,32,48}, 8 seeds, same standard-train + deep-supervision + RMSNorm recipe — the ONLY new variable is the two extra arms). The **flat loop is the PIVOT**: it already carries the full joint-state (it refines one shared latent over the whole grid = leg-1 by construction), so Δ(mixer−flat) isolates the MIXING operator on top of joint-state, while Δ(flat−decoupled) measures leg-1 without mixing. `trm_decoupled` trained fine here (acc 0.86–0.92 at 100 epochs — the M10 final-DS fragility only bit the 4-epoch smoke), so the attribution is trainability-clean. Every delta below is **8/0, p=.008**.

| w | per-arm acc (mixer / flat / ff / decoupled / untied) | Δ(mixer−flat) EM *[MIXING marginal]* | Δ(flat−decoupled) EM *[leg-1]* | Δ(flat−untied) EM *[P1]* | Δ(mixer−decoupled) EM *[total]* |
|---|---|---|---|---|---|
| 24 | 0.999 / 0.982 / 0.976 / 0.916 / 0.895 | +0.204 | **+0.577** | +0.599 | +0.781 |
| 32 | 0.998 / 0.962 / 0.950 / 0.882 / 0.862 | +0.538 | +0.378 | +0.383 | +0.916 |
| 48 | 0.998 / 0.934 / 0.889 / 0.864 / 0.814 | **+0.859** | +0.094 | +0.099 | +0.954 |

**Findings.**
1. **The win decomposes into TWO real, significant mechanisms with OPPOSITE width-scaling.** leg-1 joint-state (flat over decoupled) and P1 tying (flat over untied) are large but **SHRINK with w** on EM (+0.58→+0.09, +0.60→+0.10); the MIXING marginal (mixer over flat) is significant and **GROWS with w** (+0.20→+0.86). At small w (24) the ordinary joint-state/tying coherence the program already characterized DOMINATES (leg-1 +0.58 ≫ mixing +0.20); by w=48 the mixing operator carries almost the whole win (+0.86) and leg-1/tying have faded (+0.09).
2. **The mixer is NOT leg-1 done better — it is a DISTINCT capability that rescues the large-w regime.** The cleanest read is token-ACC: the mixer barely degrades as the ring grows (0.999→0.998) while flat (0.982→0.934), decoupled (0.916→0.864) and untied (0.895→0.814) all fall off — so Δ(mixer−flat) acc GROWS (+0.017→+0.036→+0.064) while Δ(flat−decoupled) acc is stable (~0.07) and the leg-1 EM shrink is EM-compression (both flat and decoupled degrade, so acc^w collapses for both). Mechanistically: joint-state-without-mixing (flat) loses per-cell accuracy as there are more cells to keep consistent, whereas the explicit cross-cell propagation operator does NOT degrade with w — that scaling is the new capability, and it takes over exactly at w≥32 where M9 found the flat loop's coherence edge over ff vanishes.
3. **P1 tying is BUDGET-CLEAN here** (untied within ±2% at all widths: 0.980/0.984/1.007) — Δ(flat−untied) EM +0.60/+0.38/+0.10, 8/0 — a clean tying-positive on the distractor-free converge ring. (Δ(mixer−untied) +0.80/+0.92/+0.96 conflates mixing+tying — context only; there is no untied-MIXER arm to separate them.) `coherence_excess` cross-arm Δ is uninformative here (the M9 caveat: it is confounded by token-acc *level*, and the mixer's acc≈0.998 leaves almost no acc^w "excess") — lean on EM/acc directly.

**Net (M24b).** The M24 mixer win is a genuine second mechanism, complementary to §9.2's joint-state coherence — not a re-description of it. Joint-state + tying give coherence at small w and plateau; the mixing operator gives *width-robust constraint propagation* that grows with w and rescues the regime where the flat loop's coherence runs out. So the honest updated picture of the loop's value on local-update fixed-point rings is: **joint-state coherence (leg-1/P1, established) + a cell-mixing propagation capability (M24/M24b, new) — the latter is what the flat architecture was leaving on the table.** Rigor: trainability-clean (decoupled trains to 0.86–0.92); all deltas 8/0, p=.008; budget within ±5% except mixer @ w48 (0.869, conservative under-budget — the win there is despite ~13% fewer params); num_threads=1 (mixer AND decoupled 3-D matmul). Canonical: `m24b_converge_ablation_20260703T102323_*`. Config: `configs/experiments/m24b_converge_ablation.yaml`.

### M24c + M24d — the two mixer CONTROLS bound the capability, and they REFRAME the dividing line: the mixer helps wherever outputs are CROSS-CELL COUPLED (LOCAL `converge` AND — overturning the pre-registered null — DENSE `hopfield`), and NOT where they are EXCHANGEABLE (`multi_parity`). "Cross-cell dependency," not "local rule," is the axis.

Two lean 3-arm controls (mixer vs flat vs ff, M24 recipe, mixing the only changed variable; 8 seeds, num_threads=1; distractors=0 for all arms — the mixer's `in_features % out_features == 0` constraint, kept identical so the comparison is fair). Pre-registered expectations (from M13 + §11c): `hopfield` NULL (dense, no *local* structure to propagate), `multi_parity` NULL (exchangeable outputs, no structure at all).

**M24c — `hopfield` (dense Hebbian threshold-net attractor, w∈{24,32}): the expected null is OVERTURNED — the mixer WINS, 8/0.** Δ(mixer−ff) acc **+0.046 / +0.044**, EM **+0.454 / +0.362**; Δ(mixer−flat) acc **+0.063 / +0.059**, EM **+0.496 / +0.377** — all 8/0, p=.008, budget-clean (±5%). The flat loop REPLICATES M13's failure (Δ(flat−ff) acc **−0.017 / −0.015, 0/8** — the flat loop is worse than the shallow MLP on a dense net), but the MIXER beats both. **Why the pre-registration was wrong:** the mixer's token-mix is a learned **all-to-all** linear map over cells, which matches a *dense* Hopfield coupling as naturally as a local ring — so the operator helps wherever there is cross-cell coupling, LOCAL OR DENSE. M13's "the loop's edge is CA/local-update specific, dies on a dense net" was therefore a **FLAT-loop** property (the flat update can't represent the coupling), not a property of iterative refinement — the same lesson as M23/M24. **Caveat:** distractors=0 makes this a slightly EASIER instance than M13's noise regime (ff acc 0.93 here vs M13's ff EM ~0.26), and the mixer's divisibility constraint means the harder distractor=8 instance is UNTESTABLE with the mixer — so "mixer beats flat+ff on dense coupling at matched budget" is solid, but "does it hold in M13's harder noise regime" is out of reach. The qualitative reframe (mixing is not local-specific) stands regardless.

**M24d — `multi_parity` (w INDEPENDENT k=4 parities from shared d=24 bits, w∈{4,8}): the NEGATIVE control WORKS — mixing adds NO positive marginal.** The headline statistic Δ(mixer−flat) is **≤ 0 and non-significant** (acc −0.050, 2/6, p=.29 @ w4; −0.087, 1/7, p=.07 @ w8; EM −0.033/−0.008) — the mixing operator gives nothing on exchangeable outputs, in sharp contrast to converge's +0.20→+0.86. And the mixer never beats ff (Δ(mixer−ff) acc **−0.32 / −0.23, 0/8, p=.008**): on genuinely uncoupled outputs the shallow MLP is the BEST arm and the whole loop+mixing apparatus is dead weight. **So the M24 win is STRUCTURE-specific, not generic capacity** — confirmed. Caveat: the loop arms train *unstably* on parity under this recipe (flat acc variance is large, some seeds at chance — the RMSNorm/standard-train combo is not tuned for parity), so Δ(mixer−flat)≈0 is partly "two weak arms are equal"; the airtight statement is the 0/8 mixer<ff — mixing never buys an advantage where outputs are exchangeable. (The flat loop also LOSES to ff here, −0.27/−0.14 — note this does NOT match M6a's "loop beats shallow ff on k≥4 parity"; the difference is recipe/stability, not the point of the control, which is the mixing marginal.)

**Net (M24c/M24d), and the branch-closing reframe.** The mixer's cross-cell propagation capability (M24/M24b) is bounded by **whether the target has cross-cell dependency**, not by locality: it fires on LOCAL coupling (`converge`, growing with w) AND on DENSE coupling (`hopfield`, overturning the M13-based null), and is inert-to-mildly-negative on EXCHANGEABLE outputs (`multi_parity`). This is a cleaner, more general characterization than the pre-registered "local-structure-specific" guess — and it further localizes the M13 negative as a flat-architecture artifact (the dense-coupling regime IS reachable, just not by a flat update). The one regime genuinely beyond the mixer is independent outputs, where a plain MLP already suffices. Rigor: all headline deltas 8/0 p=.008 (hopfield win; multiparity mixer<ff); budget-clean ±5% both; num_threads=1. Canonical: `m24c_hopfield_mixer_20260703T150202_*`, `m24d_multiparity_mixer_20260703T150912_*`. Configs: `configs/experiments/m24{c_hopfield,d_multiparity}_mixer.yaml`.

### M24 — ADVERSARIAL-REVIEW CORRECTION (an adversarial subagent re-derived every Δ from the CSVs and audited the code; the effects REPRODUCE but two headline ATTRIBUTIONS are walked back). The numbers are all correct and the mixer genuinely+reproducibly solves `converge` / wins on `hopfield` / is null on `multi_parity` — but (B1) NO mixing-matched non-recurrent control was run, so "the LOOP does algorithmic hard-solving" is NOT isolated from "a mixer ARCHITECTURE wins"; and (M1) M24b's "distinct mechanisms with OPPOSITE width-scaling" is an EM-FLOOR artifact that REVERSES for tying on token-accuracy.

Verified clean by the review (keep as-is): every reported Δ matches the committed CSVs in sign and magnitude; the sign tests are as stated; the mixer's converge win is real and NOT undertraining (mixer train-acc ≈1.0); the iterated depth-wall-survives claim is well-supported (all arms' *train* acc collapses to ≈baseline at T≥8, genuine unlearnability); `trm_decoupled` is trainability-clean (train/test 0.86–0.92); determinism (num_threads defaults to 1, `config.py`) and the budget audit (mixer 13.4% UNDER at w48, correctly flagged conservative) both check out.

**B1 [BLOCKER — headline overreach, §2/§4/§8]. The §4 control contract is UNMET for the mixer: no param-matched feedforward-mixer and no depth-matched untied-mixer stack was run, so `Δ(mixer−ff)` and `Δ(mixer−flat)` both confound the CROSS-CELL MIXING OPERATOR with the whole flat→mixer redesign — none isolates whether RECURRENCE / the loop contributes.** The token-mix is all-to-all, so a *single* feedforward mixer block already couples every cell; it is entirely plausible a NON-looped mixer also solves `converge`, which would make M24 an *architecture* result, not a loop result. The isolation logic used in the writeup ("it's the operator, not loop-vs-ff, because flat≈ff") is FALSE on `converge`: `Δ(flat−ff)` EM = **+0.142/+0.138/+0.083, all 8/0 p=.0078** — the flat LOOP significantly beats ff (the leg-2 coherence edge), so recurrence is NOT inert here and "flat≈ff ⇒ the delta is mixing-not-recurrence" does not hold (it held on Sudoku/iterated where flat≈ff, not here). **Corrected claim:** the defensible M24 statement is *"a weight-tied mixer LOOP beats a flat MLP and a flat loop on cross-cell-coupled ring targets."* The stronger reframe in §11 — "the loop DOES do algorithmic hard-solving once its update can mix cells" — is **NOT yet supported**; it requires a feedforward-mixer (§4a) and an untied-mixer stack (§4b). Until those run, the mixing win may be an architecture property independent of the loop. (This caveat applies equally to the M23 Sudoku mixer result, which also lacked the mixing-matched controls.)

**M1 [MAJOR — M24b central claim is a metric artifact]. "Mixing GROWS while leg-1/tying SHRINK with w ⇒ distinct mechanisms" is driven by EM FLOOR-compression and REVERSES for tying on token-accuracy.** At w48 the weak arms are on the EM floor (flat EM 0.105, decoupled 0.011, untied 0.006 — EM=acc^w collapses once acc<1), so the EM *difference* between two floored arms mechanically shrinks. On the un-compressed token-accuracy (recomputed from the curve CSV): Δ(flat−decoupled) acc = **0.065 / 0.080 / 0.070 (stable, NOT shrinking)**; Δ(flat−untied) acc = **0.087 / 0.101 / 0.120 (GROWING — the exact OPPOSITE of the "tying shrinks" claim)**; only Δ(mixer−flat) acc = 0.017 / 0.036 / 0.064 genuinely grows on both metrics. **Corrected claim:** drop the "leg-1/tying shrink with w / opposite-scaling dissociation" framing entirely — it is an EM artifact. The defensible fact is narrower and still real: **the mixer is WIDTH-ROBUST (acc 0.998→0.998) while every flat-update arm (flat/ff/decoupled/untied) degrades with w** — that alone supports "mixing is a distinct, width-robust capability," without the bogus opposite-scaling story.

**M2 [MAJOR — overstated isolation]. "The ONLY changed variable is the mixing operator" (asserted in m24_mixer_converge.yaml + m24b) is inaccurate.** flat→mixer simultaneously changes: single shared latent → per-cell latents (per-example latent STATE 24–48× larger), whole-grid MLP → token-mix + channel MLP, joint readout → shared per-cell readout, hidden_dim 64→96, and per-cell input tokenization. A clean "mixer with token_mix removed" ablation does not exist. The direction is *probably* safe (for `converge` cell_dim=1, so a no-mix per-cell model literally cannot see neighbours, and `decoupled` — per-cell latents, no mixing — loses badly), but the closest true isolation is **Δ(mixer−decoupled)** (both per-cell), NOT the Δ(mixer−flat) the M24b headline leans on. **Corrected wording:** "mixing PLUS per-cell tokenization," with Δ(mixer−decoupled) as the isolation.

**M3 [MAJOR — mechanism mislabeled]. The converge win is a RAW per-cell-accuracy win, NOT the §9.2 coherence-at-matched-accuracy mechanism the LOG says it "amplifies."** The mixer wins by being more per-cell accurate (token-acc 0.998 vs ff 0.976) which compounds over w cells into EM; its `coherence_excess` Δ vs ff is **−0.061 / −0.070 at w24/w32 (NEGATIVE — LESS excess clustering than ff)**. So it is a *different* capability from leg-2 (M9: EM edge at MATCHED token-acc). **Corrected claim:** "the mixer amplifies the loop's value exactly where the program located it" is wrong — the mixer's edge is higher per-cell accuracy that compounds to whole-row EM, distinct from the matched-accuracy coherence mechanism.

**M4 [MAJOR — negative control confounded]. M24d does not cleanly show "mixing marginal ≈ 0 on exchangeable outputs"; it shows "the mixer architecture is BROKEN on parity."** The mixer sits at/near baseline (test acc 0.570/0.514, train 0.580/0.523, baseline ~0.506) and BELOW the flat loop — the per-cell reshape (d=24 → w contiguous-bit cells) scrambles the parity inputs, actively handicapping the mixer rather than leaving it neutral. The structure-specific *direction* holds, but the clean statement is only **Δ(mixer−ff) acc −0.32/−0.23, 0/8** (mixing never buys an advantage where outputs are exchangeable), not a "marginal≈0." Also a sign-wording error: the writeup/§11 call Δ(mixer−flat) "≤0 **ns**," but at w8 the EM cell is **−0.0083, 0/8, p=.0078 (significantly negative)** — "ns" is true only on accuracy. **Fix:** either re-run with a recipe where the loop arms actually learn parity (M6a-style), or state the control establishes direction only, via mixer<ff.

**Minor.** (m2) Iterated T=8 is not literally "no edge": Δ(mixer−flat) acc = +0.033, 8/0, p=.0078 (a small significant TOKEN-acc edge over flat) — but EM=0 for all and train-acc≈baseline, so the depth-wall claim stands; say "no whole-row EM edge; the small token-acc edge doesn't cross the wall." (m1) M24c's comparison to M13 is cross-experiment (distractors 8→0); the within-M24c result is clean (ff EM 0.256/0.145, not saturated) but the "M13 was a flat-loop property" inference reinforces B1 — an all-to-all mixer winning on *dense* coupling is exactly what a strictly-more-expressive architecture would do regardless of recurrence.

**Net after correction.** M24 stands as a legitimate ARCHITECTURE finding — *a tied MLP-mixer loop beats flat-MLP / flat-loop / per-cell / untied controls on cross-cell-coupled ring targets (local `converge` + dense `hopfield`), and does not on exchangeable `multi_parity`* — with all Δs reproduced. What is WALKED BACK: (B1) that the LOOP/recurrence (vs a mixer architecture per se) is responsible — untested, needs the mixing-matched §4 controls; (M1) M24b's opposite-width-scaling dissociation — an EM-floor artifact, reversed for tying on acc; (M3) that the win is the coherence mechanism — it is raw per-cell accuracy. The decisive follow-up is a **feedforward-mixer (§4a) + untied-mixer stack (§4b)** on `converge`: if a non-looped mixer also solves it, M24 is an architecture result and recurrence is irrelevant here; if the tied loop still wins, the loop claim is earned. **[RESOLVED by M24e below — it is an ARCHITECTURE result: an untied mixer stack matches the tied loop, so "the loop does hard-solving" is walked back to "a MIXER does; tying only adds parameter-efficiency."]**

### M24e — B1 RESOLVED: the M24 converge win is a MIXER-ARCHITECTURE result, NOT a recurrence result. Built the mixing-matched §4b controls (an untied mixer stack, budget-matched + a 7× ceiling); a NON-recurrent untied mixer MATCHES the tied loop given equal capacity, so weight-tied recurrence confers NO capability — it buys only the ordinary parameter-EFFICIENCY edge (P1). "The loop does algorithmic hard-solving" is walked back to "a cross-cell MIXING architecture does; tying makes it parameter-efficient."

Built `UntiedMixerStack` (`untied_mixer` — the exact `TRMMixer` block via a shared `_MixerBlock`, stacked `n_steps` times WITHOUT weight tying, carrying `(z,a)` across blocks; ~7× params, full width — a labelled §4b ceiling) and `UntiedMixerStackMatched` (`untied_mixer_matched` — width-shrunk hidden=latent=token_hidden=w≈17–21 to the tied mixer's budget; the clean §4b tying control), registered them, and ran `trm_mixer` vs both untied arms vs `ff_matched` on `converge` (rule 78, distractors 0, w∈{24,32,48}, 8 seeds, the M24 recipe). `TRMMixer` was left untouched (the M24 results stay bit-identical); the untied classes reuse a standalone `_MixerBlock` that is byte-for-byte one `TRMMixer` step at n_latent=1. Tests added (build/shape, untied-ness, ±5% budget match, determinism); 251 tests pass, ruff clean; num_threads=1 (3-D matmul).

| w | per-arm acc (tied / untied-matched / untied-ceiling / ff) | Δ(tied − untied-ceiling) EM *[7× params]* | Δ(tied − untied-matched) EM *[budget-matched, = TYING]* | Δ(untied-matched − ff) EM *[= the MIXING operator, no tying]* |
|---|---|---|---|---|
| 24 | 0.9987 / 0.9960 / 0.9980 / 0.9764 | +0.010 (7/1, p=.07, **ns**) | +0.034 (8/0, p=.008) | +0.312 (8/0, p=.008) |
| 32 | 0.9982 / 0.9940 / 0.9975 / 0.9501 | +0.011 (6/1, p=.125, **ns**) | +0.074 (8/0, p=.008) | +0.602 (8/0, p=.008) |
| 48 | 0.9979 / 0.9859 / 0.9962 / 0.8891 | +0.048 (7/1, p=.07, **ns**) | +0.289 (8/0, p=.008) | +0.653 (8/0, p=.008) |

**Findings.**
1. **The hard-solving is the MIXING OPERATOR, essentially independent of recurrence.** A budget-matched UNTIED mixer stack (no weight tying) already beats the flat MLP by EM **+0.31/+0.60/+0.65 (8/0)** — i.e. it captures the bulk of M24's headline Δ(mixer−ff) (+0.35/+0.68/+0.94). And the 7×-param full-width untied ceiling essentially TIES the tied loop (acc 0.998 vs 0.998; **Δ(tied−ceiling) EM ns at every w**, p≥.07). So given equal-or-more capacity, a non-recurrent mixer solves `converge` as well as the tied loop — the win is the cross-cell propagation architecture, not the loop.
2. **Weight-tied recurrence buys only PARAMETER EFFICIENCY (P1), not a capability.** At fixed budget the tied loop beats the untied-matched stack — **Δ(tied−untied_matched) EM +0.034/+0.074/+0.289, 8/0, p=.008, GROWING with w** — the exact tying-positive the flat program found (M8c/M9): tying lets one budget buy the depth an untied stack must spend width to afford. But this edge is small next to the architecture win, and it VANISHES against the capacity-unconstrained ceiling (ns) — so tying is an efficiency trick, not an algorithmic capability the untied mixer lacks.
3. **This mirrors M21 / the whole-program read of recurrence.** On the FLAT loop the program found the same shape: tying beats a fair untied stack (P1) yet the loop is "dressed-up depth" (M21 — no genuine fixed-point iteration). The mixer changes the ARCHITECTURE's power (cross-cell propagation the flat MLP cannot express), but recurrence PER SE remains what it always was here — a parameter-efficiency device, not iterative algorithmic search.

**Corrected headline.** M24's "the loop DOES do algorithmic hard-solving once its update can mix cells" (and the parallel M23-mixer Sudoku reframe) is **walked back**: *a cross-cell MIXING architecture does the hard-solving on coupled ring/grid targets; weight-tied recurrence adds the ordinary parameter-efficiency edge (P1, growing with w) but no capability an untied mixer of equal capacity lacks.* The B1 blocker is resolved — and it resolves toward **architecture, not recurrence**, which is the honest and more conservative reading. (This does NOT touch the §9.2 legs — leg-1 joint-state and P1 tying stand — nor the depth-extrapolation negatives, which the mixer never cracked, M24 iterated.) Rigor: untied_mixer_matched budget-clean (±3–5% of tied); untied_mixer the exempt 7× ceiling; ff at w48 is 1.16× OVER the (small) tied-mixer budget and still loses massively (conservative); all §4b deltas 8 seeds, exact sign test; num_threads=1. **Caveat:** this tests TYING (untied-vs-tied), which is the §4b axis the reviewer's B1 targeted; a pure single-pass feedforward mixer with NO answer-carry (a stricter §4a) was not built, but the untied stack already establishes a non-recurrent mixer suffices, so that variant would only reinforce the verdict. Canonical: `m24e_converge_mixer_untied_20260703T235935_*`. Config: `configs/experiments/m24e_converge_mixer_untied.yaml`; models `UntiedMixerStack`/`UntiedMixerStackMatched` in `src/looptab/models/mixer.py`.

---
