# M29 — DONE (FINAL verdict after TWO adversarial reviews; the intermediate "m29c reversal" was itself over-claimed and is RETRACTED). Re-test the DEEP-SUPERVISION MECHANISM on the cross-cell mixer. **FINAL: M18's "the detached-carry deep-supervision MECHANISM is inert; the N_sup gain is just more optimization" verdict HOLDS on the mixer — and M29c, properly analyzed, STRENGTHENS it (the carry is inert even in a headroom regime), it does NOT reverse it.** Trail: (1) m29a/m29b found the carry inert/harmful on the mixer but with NO optimization headroom (review-1 MAJOR 1: inconclusive — can't tell "mechanism inert" from "nothing helps a fitted model"). (2) M29c found the one headroom regime (`iterated` T6, non-convergent) and I FIRST read it as a reversal (carry "+0.054, 8/0"). (3) Review-2 DISMANTLED that: the +0.054 is a non-robust **2-seed ceiling rescue** (seeds 1,3 = +0.21; the other 6 are within one test-cell of a tie at ceiling; median ≈ 0.0004; drop the ceiling-ties → 4/4 → **p=0.125, BELOW §5's significance floor**). The robust gain is the compute-matched **no-carry** arm (+0.185) = 4 consecutive gradient steps/batch = *batch-repetition COMPUTE*, which is exactly M18's "compute, not mechanism." So M29c UPHOLDS M18 (carry mechanism inert even given headroom); on the convergent target where the carry is actually motivated (m29a converge) it is −0.112 (HARMFUL). The only genuinely new wrinkle is narrow and not the DS mechanism: on this one non-convergent operator the compute is not deliverable as reshuffled 4× epochs (a batch-ORDERING effect, no isolating control, bimodal training). NO code blocker throughout; both the "reversal" and this retraction are scientific corrections.

**Why this milestone.** Every DS finding in the repo — the M18 "DS mechanism is inert" verdict included — was measured on the FLAT `trm`, which M23/M24 showed is ≈ a feedforward. Since `trm_mixer` landed, DS has only ever been HELD FIXED as an ingredient of the M24 recipe (`deep_supervision: true`), never ablated. By the same logic that overturned the M23/M24/M24f negatives, the M18 DS verdict deserved a mixer re-test. Two distinct senses of "deep supervision": (A) the per-step-READOUT DS flag (`deep_supervision`, weights `all_logits`); (B) the canonical detached-carry N_sup MECHANISM (`train_deep_supervision`, the ARC-autopsy "active ingredient" M18 studied — an outer loop of `n_sup` passes carrying `(z,a)` DETACHED between them). Both re-tested.

**One code change (additive, off-by-default ⇒ M0–M28 bit-identical).** `run.py`'s `n_sup>1` dispatch gate excluded the mixer by allowlist (`arm.name not in ("trm","trm_decoupled")`), a deliberate but now-outdated restriction — `TRMMixer.forward` already exposes the full `init_state`/`return_state`/`n_steps` API `train_deep_supervision` drives, and its per-cell `(z,a)` state detaches element-wise exactly like the flat loop's. Widened the tuple to include `"trm_mixer"` (the sibling M27 `train_stable` gate already whitelisted it). No committed config sets `n_sup>1` on `trm_mixer`, so zero existing results change. 5 tests added (`tests/test_ds_mixer.py`): routine runs on the mixer, deterministic at `num_threads=1` (the 3-D-matmul caveat), the `carry` flag is genuinely wired (carry vs no-carry diverge), end-to-end `run_point` dispatch, and the non-loop guard still raises. 275 tests pass; ruff clean.

**Design — SATURATION is the interpretive risk (the M27 trap).** A DS null is only meaningful if the pivot has HEADROOM. On plain `converge` the mixer SOLVES at 100 epochs (M24), so a null there would be vacuous. Two UNSATURATED regimes: (1) `converge` rule 78 w24 distractors=0 UNDERTRAINED to `epochs=25` (screened: `mixer_nods` EM 0.86 ≪ the 0.97 100-epoch ceiling — headroom, and the mixer is genuinely working at token-acc 0.99, not undertrained-to-noise); (2) `nested_converge` @4k w24, where the mixer PLATEAUS at EM 0.78 even at full 100-epoch training (M28) — EM headroom WITHOUT undertraining. **[Correction, per MAJOR 1 below: this is EM/DATA headroom, NOT optimization headroom — the mixer already fits TRAIN (train_acc 0.994; 4×-epoch → train_acc 1.0, test EM ~flat), so nested is a DATA-bound plateau, not the "decisive" optimization test billed here.]** Both run the M18 decomposition on `trm_mixer` AND replicate it on the flat `trm` IN THE SAME RUN (matched task/epochs), so the flat-vs-mixer contrast is one experiment. Pivot = `mixer_nods` (DS off, n_sup=1), as M18e pivoted on `trm_nods`. 8 seeds, exact sign test, budget-clean ±5%, `num_threads=1`. The 4×-epoch "is N_sup just more epochs?" control (M18f-style) is a separate config each (epochs is a global TrainConfig field). Configs `m29a_ds_mixer_converge{,_4xep}.yaml`, `m29b_ds_mixer_nested{,_4xep}.yaml`.

**Results (per-arm EM; every Δ is 8 seeds, EM, exact sign test).**

_Part A — per-step-READOUT DS on the mixer: NOT robustly load-bearing._ Δ(mixer_ds − mixer_nods) = **+0.075, 8/0, p=.008 on CONVERGE but +0.010, 5/3, p=.73 (ns) on NESTED**. The converge "win" is at the token-acc ceiling (0.99) with `coherence_excess` ≤0 (a raw near-ceiling EM effect, not coherence); where the mixer has genuine EM headroom (nested) the per-step DS flag is INERT. So the DS flag baked into the M24 recipe is not doing load-bearing work for the mixer.

_Part B — the detached-carry N_sup MECHANISM on the mixer: INERT, both regimes (M18 holds)._ Pivot `mixer_nods`:
- raw 4× compute (no-carry) HELPS: Δ(mixer_nsup_nocarry − mixer_nods) EM **+0.107 (converge, 8/0) / +0.0415 (nested, 8/0)**, both p=.008.
- the CARRY adds NOTHING: Δ(mixer_nsup − mixer_nsup_nocarry) EM **−0.112 (converge, 1/7, ns — high variance, destabilizing) / −0.009 (nested, 3/5, ns)**.
- plain 4×-EPOCH matches-or-beats N_sup: converge `mixer_nods_4xep`@100 EM **0.974** ≈ no-carry@25 (0.965) and BEATS carry-N_sup@25 (0.853); nested `mixer_nods_4xep`@400 EM **0.801** ≈ the N_sup arms@100 (0.813/0.822) — all on the same ~0.80 nested plateau (the M18j shared-capacity wall).
→ On the mixer the detached-carry deep-supervision MECHANISM is inert; the entire N_sup gain is more optimization, delivered most simply as more epochs. **M18's verdict is upheld on the mixer** — cleaner, even, since the carry is inert-to-DESTABILIZING (converge nsup acc 0.989±0.015, seeds drop) rather than the flat loop's small positive.

_Flat replication + the ARCHITECTURE CONTRAST (the one new finding)._ On the FLAT loop the carry HELPS: Δ(flat_nsup − flat_nsup_nocarry) EM **+0.129 (converge, 8/0) / +0.038 (nested, 7/0)**, both sig; and carry-N_sup BEATS plain 4×-epoch (converge flat 0.624 vs 0.485; nested flat 0.805 vs 0.734). Full flat N_sup win large: Δ(flat_nsup − flat_nods) EM +0.380 (converge) / +0.131 (nested), 8/0. So the detached carry is a REAL effect on the flat loop in these undertrained/distractors=0 regimes — a departure from M18's own flat null (+0.012 ns), which was distractors=8 / 100-epoch base (i.e. LESS undertrained). **A DESCRIPTIVE architecture split (CONFOUNDED with operating point — see MAJOR 2 in the review below): the carry helps the weak flat loop (≈ ff, which has optimization headroom) and not the mixer (nearer its ceiling in both regimes).** The "the mixer converges within one pass so the carry is redundant" reading is consistent with the program's IN-OPERATOR finding (M23/M24, vs iteration/depth M21) BUT is itself an operating-point argument — architecture and headroom are not separated here, so this is a description, not a clean dissociation.

**Net verdict (as corrected by the adversarial review below — read that block; the BROAD headline is walked back).** In the two regimes tested, the detached-carry deep-supervision mechanism does NOT help the mixer — it is null-to-DESTABILIZING on converge and inert on nested — while it helps the flat loop; and the N_sup gain that does exist is raw compute (matched by plain 4×-epoch). This is *consistent with* M18's "the carry mechanism is inert; the gain is just more optimization," but is NOT a decisive re-establishment of it on the mixer, because **neither regime gives the mixer genuine OPTIMIZATION headroom** (converge near-solved; nested is train-fit and DATA-bound — the M18j wall), so the mixer carry-null cannot separate a genuinely-inert DS mechanism from "no optimization lever helps the mixer here." Per-step-readout DS is likewise not robustly load-bearing for the mixer. Canonical: `m29a_ds_mixer_converge_20260708T095050_*` (+ `_4xep_20260708T162954`), `m29b_ds_mixer_nested_20260708T155324_*` (+ `_4xep_20260708T190109`). Config: `configs/experiments/m29{a,b}_ds_mixer_*.yaml`.

### M29 — ADVERSARIAL REVIEW CORRECTION (a subagent re-derived every Δ from the CSVs, audited the code + tests, and hunted confounds). NO BLOCKER — code faithful (the mixer's per-cell `(z,a)` genuinely carries + detaches across passes; `deep_supervision:false` ⇒ pure N_sup), bit-identity TRUE (no committed config sets `n_sup>1` on `trm_mixer`; the widened allowlist only turns a prior *raise* into a *run*), 5 tests meaningful and passing, and EVERY number reproduces (per-arm EM means + independently-recomputed accuracy sign-counts/p match the CSVs bit-for-bit — no cherry-pick). But THREE interpretive over-reaches are walked back:

- **MAJOR 1 — "nested is the decisive regime" is WRONG; neither regime gives the mixer OPTIMIZATION headroom, so the mixer carry-null is under-informative.** N_sup/DS is *more (effective) optimization*; a null is only informative where more optimization *could* help the mixer. It can't in either: converge `mixer_nods` token-acc **0.990** (near-solved, 4×-epoch EM 0.974); nested `mixer_nods` **train_acc 0.994** and the 4×-epoch control reaches **train_acc 1.000 with test EM barely moving 0.780→0.801** — the mixer already fits train and sits on the M28/M18j **data-bound** ~0.79 plateau. So the raw-compute delta is tiny (nocarry−nods EM +0.0415 nested), and the carry-null is measured where NOTHING helps the mixer — *by construction*, independent of any "DS mechanism" claim. Nested is DATA-bound, not optimization-bound → not "decisive." **The BROAD claim "the DS mechanism is inert on the architecture that does real algorithmic work" is OVER-REACH** until a mixer regime with genuine optimization headroom is tested and still shows a carry-null; the defensible NARROW claim is "in these two regimes the carry does not help — and destabilizes — the mixer, unlike the flat loop."
- **MAJOR 2 — the "clean architecture split" is CONFOUNDED with operating point.** "Carry helps flat, not mixer" co-varies perfectly with headroom: the mixer is the higher-accuracy / lower-headroom arm in BOTH regimes (converge 0.990 vs flat 0.916; nested 0.933 vs flat 0.879), and only the flat loop has optimization headroom (its 4×-epoch improves *test*). The writeup's own mechanism ("the mixer converges within one pass, so the carry is redundant") IS an operating-point argument, not an architecture-independent property. So "carry helps whoever is further from ceiling" is not excluded — relabel the split DESCRIPTIVE, not a clean architecture dissociation.
- **MAJOR 3 — on converge the carry is "null-to-HARMFUL," not "inert."** Δ(mixer_nsup − mixer_nsup_nocarry) converge = −0.112 EM, **7/8 seeds worse, p=0.070** (nearly sig in the NEGATIVE direction; nsup seeds dip to 0.958/0.974 vs a rock-steady 0.998 nocarry). The symmetric top-line word "inert (both regimes)" understates converge → "null-to-harmful/destabilizing on converge; inert on nested."
- MINORs (noted, not load-bearing): the flat "carry helps" result contradicts M18's flat null (+0.012 ns) but M29 changes distractors 8→0 AND epochs AND n_train together — not a one-knob isolation (§5), so the regime attribution is asserted not isolated (low severity — the core mixer claim is shown directly, not via the flat contrast); the "just more epochs" comparison is cross-run + mean-only (unpaired, indicative per M18f precedent — seeds/task/arch verified identical); per-seed EM is NOT persisted in the JSON (`accuracy_per_seed` is, `exact_match` only mean/std) so the EM sign-counts can't be re-derived from committed artifacts — an audit gap worth closing (persist `exact_match_per_seed`) since EM is the headline metric; budget tol was ±5% (mixer +4.1% over flat), looser than the usual ±2%. With MAJOR 1/2/3 folded in, the milestone is defensible on its NARROW claim; the broad "M18 holds on the mixer" is downgraded to "consistent with M18, not decisively established (no mixer optimization-headroom regime tested)."

### M29c — the review's requested OPTIMIZATION-HEADROOM test (MAJOR 1). **[⚠ THE "REVERSAL" HEADLINE OF THIS BLOCK IS RETRACTED by a SECOND adversarial review — see the correction block immediately below; the corrected reading is that M29c UPHOLDS M18. The block is kept for the reasoning trail; read it together with its retraction.]** As first (over-)read: on the one regime where the mixer under-fits train (`iterated` T6), the detached-carry N_sup mechanism looked like a large help that plain 4× epochs cannot replicate, "falsifying" M18. That reading did not survive scrutiny (below).

**Finding the regime (a result in itself).** To give the mixer genuine OPTIMIZATION headroom (train_acc < 1, so more optimization *could* help — the thing m29a/m29b lacked), screened `mixer_nods` train-fit across harder cross-cell tasks. **On EVERY convergent fixed-point task the mixer FITS TRAIN** (train_acc ≥ 0.99: converge w24/32/48, rule 13, n_steps 3–8; mixed_converge w32; nested_converge n_blocks=5 w40 at 8k AND 16k). So a "mixer optimization-headroom" regime does NOT exist on the convergent tasks where DS is classically relevant — the mixer simply solves them, which is *itself why* N_sup is inert there. The ONLY regime where `mixer_nods` genuinely under-fits is the NON-CONVERGENT moving CA `iterated` rule 110 at **T=6** (train_acc 0.80, test 0.76 — above baseline 0.54, a partially-learnable operator, NOT the fully-unlearnable T≥8 wall). Ran the full m29a/m29b decomposition there (config `m29c_ds_mixer_iterated{,_4xep}.yaml`, 8 seeds, `num_threads=1`, budget-clean ±5%). **Metric = TOKEN-ACCURACY** (EM floored: token 0.76 over w=24 ⇒ EM≈0); `n_steps=8 > T=6` so depth is NOT the constraint — the under-fit is operator-LEARNING (genuine optimization headroom).

**Result (token-acc, the metric; 8 seeds, exact sign test):**
| arm | routine | train | test |
|---|---|---|---|
| `mixer_nods` | 1 pass, 100 ep (pivot) | 0.800 | 0.761 |
| `mixer_nods_4xep` | 1 pass, **400 ep** | 0.842 | **0.796** |
| `mixer_nsup_nocarry` | 4-pass N_sup, no carry, 100 ep | 0.957 | 0.946 |
| `mixer_nsup` | 4-pass N_sup, **carry**, 100 ep | **1.000** | **1.000** |

- **N_sup ≫ plain epochs (M18's "just more epochs" FALSIFIED here).** Same optimizer-step budget (n_sup=4 × 100 ep = 400 pass-updates = 1 pass × 400 ep), yet the N_sup routine breaks the plateau (test 0.76 → 0.95 no-carry → 1.00 carry) while plain 4× epochs barely moves it (0.76 → 0.80, train 0.80 → 0.84 — still stuck). Δ(mixer_nsup − mixer_nods) acc **+0.239, 8/0, p=.008**; Δ(mixer_nsup_nocarry − mixer_nods) [pass-structure alone] **+0.185, 8/0**. So the segmented detached-supervision *routine* — not raw compute — escapes the optimization plateau plain SGD gets stuck in (a Deep-Thinking-style effect, §12; note M7 found progressive loss INERT on the FLAT loop — here the MIXER + detached N_sup cracks a hard operator the flat loop could not).
- **The CARRY helps the mixer, significantly (m29a/m29b "carry inert on the mixer" FALSIFIED here).** Δ(mixer_nsup − mixer_nsup_nocarry) acc **+0.054, 8/0, p=.008** — every seed the carry ≥ no-carry; the carry arm SOLVES uniformly (1.000 ± 0.0001) while no-carry is unstable (0.946, 3/8 seeds stuck ~0.79). The carry is the last-mile + stability that turns "mostly solves" into "solves."
- Per-step-readout DS also helps here (Δ(mixer_ds − mixer_nods) +0.199, but 6/2, p=.29 ns — high variance, one seed 0.71). Flat loop: N_sup helps a little (+0.067 8/0) but the flat ≈ ff operator stays near the M3a wall (~0.63, never solves) — the mixer OPERATOR is what makes the task learnable at all.

**Corrected M29 verdict [ITSELF RETRACTED — see the SECOND REVIEW block below].** As first written this block claimed the M18 verdict is "HEADROOM-DEPENDENT" and that M29c reverses it. That claim is withdrawn. Canonical: `m29c_ds_mixer_iterated_20260709T133451_*` (+ `_4xep_20260709T184421`); config `m29c_ds_mixer_iterated{,_4xep}.yaml`.

### M29c — SECOND ADVERSARIAL REVIEW: the "reversal" is WALKED BACK; M29c actually UPHOLDS (strengthens) M18. A cold reviewer re-derived every per-seed number from the JSON and found the reversal fell into the SAME ceiling-saturation trap the FIRST review flagged (its MAJOR 3), now in the opposite direction. Verified independently (per-seed `accuracy_per_seed` from `m29c_...133451.json`):

- **BLOCKER 1 — the carry "+0.054, 8/0" is a non-robust 2-SEED ceiling rescue that FAILS §5's significance floor.** Per-seed carry(`mixer_nsup`) − nocarry(`mixer_nsup_nocarry`): seed1 **+0.206**, seed3 **+0.214** (these two ARE the effect); seed0 +0.009, seed4 +0.0007; **seeds 2/5/6/7 = +0.00004 each = ONE test-cell out of 24,000** (both arms at ceiling). **Median ≈ 0.0004.** The "8/0" counts those 4 one-cell ceiling differences as positive votes; drop the ceiling-ties → informative seeds {0,1,3,4} = 4/4 → two-sided sign **p=0.125 (> 0.05)**. So the carry is NOT a significant help — it is a variance rescue of the 2 seeds `nocarry` left stuck at ~0.79.
- **BLOCKER 2 — the robust gain is COMPUTE, and by M18's own decomposition that UPHOLDS M18.** The compute-matched no-carry arm (Δ vs pivot **+0.185, 8/0**) is, by the routine's definition (`train_deep_supervision` `carry=False`, loop.py:165–183), literally *4 consecutive gradient steps on the same batch with fresh state* — batch-repetition compute, not the deep-supervision *mechanism*. M18's decomposition is exactly "nocarry = compute (real), carry = mechanism (inert)"; M29c reproduces that structure (compute +0.185 real, carry +0.054 non-robust). So M29c CONFIRMS M18; it does not reverse it — and now does so IN a headroom regime, answering review-1's MAJOR 1 (even with headroom, the carry mechanism is inert on the mixer).
- **MAJOR — the one non-M18 wrinkle is batch-ORDERING, not the mechanism, and is bimodal.** `mixer_nsup_nocarry` (0.946) beats `mixer_nods_4xep` (0.796), i.e. 4 consecutive steps/batch beat reshuffled 4× epochs — but that is a data-ordering/optimization-schedule effect with NO isolating control (no 1-pass consecutive-repetition or LR/step-matched arm), and the training is bimodal: `mixer_nods` seed6 SOLVED (0.998) yet `mixer_nods_4xep` seed6 = 0.645 (more epochs *destroyed* the solved seed), and on the 2 plateau seeds (1,3) plain 4×-epoch actually BEAT nocarry (0.81/0.83 vs 0.79). So "N_sup escapes a plateau 4× epochs can't" is ceiling-counting over an unstable landscape, not a mechanism result.
- **Context also against the reversal:** the carry is −0.112 (HARMFUL) on the convergent target where it is actually motivated (m29a converge); the m29a(+0.112 destabilize)→m29c(+0.054) sign flip runs OPPOSITE to fixed-point-refinement theory (consistent with noise); token-acc only (coh_excess Δ(carry−nocarry) −0.002 ns — no coherence effect). Code/determinism/budget clean (num_threads=1, distractors=0, faithful carry wiring), so this is purely an interpretation walk-back.

**Net (M29c, corrected):** M29c does NOT reverse M18 or the m29a/m29b mixer carry-null — it STRENGTHENS them: even in the one mixer optimization-headroom regime, the detached-carry *mechanism* is inert (2-seed ceiling rescue, p=0.125), while the *compute* (no-carry batch-repetition) carries the gain — exactly M18. The only defensible new observation is narrow and not the DS mechanism: on this one hard NON-CONVERGENT operator the optimization is not deliverable as reshuffled epochs (a batch-ordering curiosity, uncontrolled, bimodal). The first-draft M29 reading ("carry inert/harmful on the mixer, consistent with M18") was correct; the intermediate m29c "reversal" was the ceiling-saturation trap and is retracted.
